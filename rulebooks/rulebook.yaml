---
- name: Listen for Webhook Events
  hosts: all
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000
  rules:
    - name: Respond to a payload
      condition: event.payload.message == "Hello"
      action:
        debug:

