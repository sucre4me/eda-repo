---
rules:
  - name: Truncate /var files if usage exceeds 90%
    conditions:
      - event_type: ansible_facts
        fact: ansible_mounts
        operator: contains
        value:
          mount: "/var"
          percent: ">=90"
    actions:
      - name: Trigger truncate workflow
        workflow: truncate_var_workflow.yml
